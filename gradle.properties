# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G
org.gradle.caching=false
org.gradle.caching.debug=false
org.gradle.parallel=false

# Mod properties
mod.version=0.1.0
mod.group=ch.krishd
mod.id=chunkpermits
mod.name=Chunk Permits

# Used for the mod metadata
mod.mc_dep_fabric=[VERSIONED]
mod.mc_dep_forgelike=[VERSIONED]
mod.mc_title=[VERSIONED]
mod.mc_targets=[VERSIONED]

# Mod setup
deps.mixin_extras=0.4.1
deps.fabric_loader=0.18.4
deps.fabric_version=[VERSIONED]
deps.neoforge_loader=[VERSIONED]
deps.architectury_api=[VERSIONED]
deps.sqlite_jdbc=3.46.1.3

# Optional Fabric integrations
deps.modmenu_version=[VERSIONED]

# Publishing
publish.modrinth=
publish.curseforge=
